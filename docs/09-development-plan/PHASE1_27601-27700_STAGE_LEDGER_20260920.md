# Stages 27601–27700 Ledger

- Calibrated placement runtime validates finite component/measurement inputs and reuses the existing AffineCalibrationValidationRuntime before transformation.
- The measured board-space point is then passed into PcbPlacementObservationRuntime, producing real expected-vs-measured geometry.