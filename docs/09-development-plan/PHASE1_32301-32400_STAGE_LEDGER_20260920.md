# Stages 32301–32400 Ledger

- Added executable Smoke using real RecordingFrameSource + ProductionSessionRuntime and two real ViewportRenderPipelineRuntime instances.
- Covered source payload identity, capture dimensions, render fingerprints, capture metadata tampering, and changed-render-summary rejection.