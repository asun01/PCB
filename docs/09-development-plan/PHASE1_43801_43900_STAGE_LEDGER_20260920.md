# Phase 1 — Stages 43801–43900

Closed invalid-state coverage for Simulation → Render replay handoff.

- Duplicate simulation/render sequence detection added at source validation.
- Fingerprint width validation prevents malformed replay identities from entering the descriptor.
- The acceptance smoke exercises both malformed-source and malformed-descriptor paths.