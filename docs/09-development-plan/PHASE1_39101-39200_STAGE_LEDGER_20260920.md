# Phase 1 — Stages 39101–39200 Ledger

- Chain: Audit → Release replay identity.
- Runtime asset: `PcbAuditReleaseTransitionRuntime.CreateTransitionKey`.
- Acceptance: produces a deterministic SHA-256 transition key from envelope, Quality, audit-window, and Release identities.
- Invalid-state handling: null projection is rejected.
- Smoke: `PcbAuditReleaseTransitionRuntimeHundredStageSmoke`, exact 100 numbered rounds.
- Static checks: deterministic key, fixed-width digest, ten loop groups, ten Check call sites, explicit `round==100`.
- CI/build boundary: no authoritative workflow result asserted.
