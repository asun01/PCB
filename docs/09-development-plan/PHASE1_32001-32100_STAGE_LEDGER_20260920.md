# Stages 32001–32100 Ledger

- Product chain: Acquisition / Production provenance → Render replay.
- Added ProductionCaptureRenderProvenance to retain source payload SHA, capture dimensions/pixel format, render generation, render summary, and render fingerprint in one factual record.