# Development Documentation

The intended v2.1.5 repository layout contains baseline, governance, platform, vision, metrology, UI, domains, AI, qualification, development-plan, audit, FunctionSpec, ContractCandidate, PageContract and reference documents.

The master document is a convenience aggregation; individual authoritative document units remain the implementation source of truth.
