# 审计规则变异测试与检测能力审计
- 文档 ID：`AUD-075`
- 版本：`1.0.0`
- 状态：`PASS_WITH_EXTERNAL_GATES`
- 审计基线：`ARCH-PCBA-VISION-001 v0.5.3` + `Asun Vision Development Standard v2.1.5`

本审计不是重新阅读文档，而是对审计器本身做“故障注入”：

- 故意篡改 Registry SHA-256；
- 故意删除 FS 的 SideEffectPolicy 行；
- 故意修改 Primary Work Package；
- 故意将 README 改回旧版号；
- 故意破坏 Master END 标记；
- 故意制造重复 Document ID。

要求：每一种篡改都必须被独立结构审计识别并返回非零退出码；只有原始包全部通过时才能进入发布封版。

结论：上述变异均被当前独立结构审计识别；审计器本身不能被“看似通过”的静态结果替代。
